import { OnModuleDestroy, OnModuleInit } from "@nestjs/common";
import { ConfigService } from "@nestjs/config";
import { PushNotificationsService } from "../alerts/push-notifications.service";
import { PrismaService } from "../prisma/prisma.service";
export declare class BudgetGoalJobsService implements OnModuleInit, OnModuleDestroy {
    private readonly prisma;
    private readonly config;
    private readonly pushNotifications;
    private readonly logger;
    private budgetAlertsTimer;
    private goalAlertsTimer;
    private billAlertsTimer;
    private recalcTimer;
    private activeBudgetAlertKeys;
    private activeGoalAlertKeys;
    private activeBillAlertKeys;
    private activeSpendingSpikeAlertKeys;
    private lastRecalcSummary;
    private readonly jobsEnabled;
    private readonly budgetAlertIntervalMinutes;
    private readonly goalAlertIntervalMinutes;
    private readonly recalcIntervalMinutes;
    private readonly goalDueSoonDays;
    private readonly spendingSpikeThresholdPercent;
    constructor(prisma: PrismaService, config: ConfigService, pushNotifications: PushNotificationsService);
    onModuleInit(): void;
    onModuleDestroy(): void;
    private fetchCurrentMonthBudgetUsage;
    private fetchOpenGoals;
    private fetchExpenseSpikeRows;
    private getBudgetAlerts;
    private getGoalAlerts;
    private getSpendingSpikeAlerts;
    private toPersistedBudgetAlert;
    private toPersistedGoalAlert;
    private toPersistedSpendingSpikeAlert;
    private persistAlerts;
    private runBudgetAlerts;
    private runSpendingSpikeAlerts;
    private getBillDueDate;
    private runBillAlerts;
    private runGoalAlerts;
    private runRecalculation;
}
